# Candidate Ledger Specification — Our Lovely System

**Date:** 2026-08-21

## PRIMARY CONTROL INPUTS

The user initiated a journal session:

> Computahhh it's juhhhnal tyimmme.

After asking Computahhh to present the journal program, the user established context:

> Context Our Lovely System.

The user then asked:

> We had a design discussion earlier. Confirm.

Computahhh confirmed that an earlier design discussion concerning Our Lovely System had occurred.

The user instructed:

> Present candidate specifications as discussed and as agreed.

Computahhh reconstructed the candidate specification, distinguishing the deliberately narrow first pass from directional or future capabilities.

The user then instructed:

> Log it.

This event is the resulting record.

## CANDIDATE SPECIFICATION AS PRESENTED

| Area | Agreed Pass 1 | Direction / Later |
|---|---|---|
| Content | **Rich Markdown journal entries** | — |
| Entry identity | **Stable `secuuid` per entry** | Used as relational key across other resources |
| Canonical store | **S3** | Remains durable authority |
| DynamoDB | **Operational projection** | Search/access projections may grow |
| DynamoDB authority | **Not authoritative** | Must be reconstructible from S3 |
| Querying | **DynamoDB for predetermined access patterns** | Athena for richer/ad-hoc queries |
| Search | **Performance, paging, searchability required** | More sophisticated search later |
| Athena | **Candidate query component** | Especially keyword/richer historical queries |
| Attachments | **Not Pass 1** | `attachments/<secuuid>/...` |
| Comments | **Not Pass 1** | `comments/<secuuid>/...` |
| Tags | **Explicitly not Pass 1** | `tags/<secuuid>/...` |
| Tag reputation | **Not Pass 1** | Durable/decaying tags, reputation metrics |
| Voting/governance | **Not Pass 1** | Separate system interacting with ledger |
| OpenSearch | **Not selected** | Reconsider if rich interactive search earns its cost |
| Replication | **S3 → event → Lambda → DynamoDB projection** | Needs independent reconciliation |
| Failure semantics | **DynamoDB loss must not destroy S3 record** | Projection should be repairable |
| Reconciliation | **Requirement identified; design unresolved** | Independent verification of S3↔DynamoDB correctness |
| S3 organization | **Resource-type-first separation** | Avoid mixing attachment binaries into Athena-facing journal data |

## ARCHITECTURAL SHORTHAND AS PRESENTED

```text
S3
├── journal/<secuuid>
├── attachments/<secuuid>/...    [later]
├── comments/<secuuid>/...       [later]
└── tags/<secuuid>/...           [later]

             S3
              │
              │ event
              ▼
           Lambda
              │
              ▼
          DynamoDB
       (projection/cache)

Athena ──────────────► S3
        ad-hoc/richer queries
```

Computahhh summarized the strongest agreed invariant as:

> **S3 remembers. DynamoDB accelerates. DynamoDB does not become truth merely because it is convenient.**

Computahhh also preserved an important qualification: the discussion identified but did not solve the replication-integrity problem. Continuous projection explains how DynamoDB gets populated; it does not establish that DynamoDB remains a faithful representation of S3.

## STATUS

This record captures the candidate specification as reconstructed and presented during the journal session. It deliberately distinguishes agreed Pass 1 scope from future direction and leaves the reconciliation implementation unresolved rather than manufacturing a decision that was not made.